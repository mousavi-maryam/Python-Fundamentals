"""
Chapter 6 - Exercise 4

Objective:
Use Python's built-in `count()` string method to determine how many times a specified 
character appears in a string.

My Solution:
Created a string variable containing the word `"banana"` and a variable for the target letter. 
Used the built-in `count()` string method to count the occurrences of the specified letter 
and displayed the result.
"""




word = "banana"
letter = "a"

print(word.count(letter))